#include "main.h"
/**
 * add - a function that adds two numbers
 * @a: a number to be added
 * @b: second number to be added
 * Return: the numbers added
 */
int add(int a, int b)
{
return (a + b);
}
