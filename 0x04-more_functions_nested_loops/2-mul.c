#include "main.h"
/**
 * mul - A function that multiplies two integers
 * @a: first int type number
 * @b: second int type number
 * Return: multiples ot two integer
 */
int mul(int a, int b)
{
return (a * b);
}
